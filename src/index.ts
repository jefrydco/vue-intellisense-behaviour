/**
 * Convert kebab case to camel case
 * @param inputString The input string
 * @example
 *
 * ```javascript
 * cmCamelize('hello-world')
 * // helloWorld
 *
 * cmCamelize('helloWorld')
 * // helloWorld
 * ```
 */
export function cmCamelize(inputString: string): string {
  return inputString
}
